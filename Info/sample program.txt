mvar a = 10;
mvar b = 20;

Add a to b;
Sub a to b;
Multiply a to b;
Divide a to b;

mprint(a);
mprint(b);

mvar array = ["hi", "bruh", "e"];

array.show_all();
array.show_at(2);
array.remove_at(3);
array.insert("hello", at: 4);

array.show_all();

mvar new_file = Create.File((name)'txt');
mvar new_folder = Create.Folder((name));

new_file.write("Hello, mii!");
new_file.read();

new_folder.create_file((name)'txt');
new_folder.read();

mfunc add_numbers(x, y):
    result = x add y;
    mprint(result);

mrun add_numbers(run for 2);

mfunc user_input():
    text = AskInput("Enter text:");
    mprint(text);

    name = AskInput("Enter your name:");
    mprint(name);

user_input();

